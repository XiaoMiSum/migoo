# mongo 默认配置，各配置项的优先级为：取样器 > 默认配置
testclass: mongo_defaults # 配置元件类型
config:
  acks: 1 # 可空
  retries: 5  # 可空
  linger.ms: 20   # 可空
  bootstrap.servers: 127.0.0.1:9092 # 服务器地址
  topic: ryze.topic  # ProducerRecord 中的 topic
  key: ryze # ProducerRecord 中的 key