name: 测试使用mybatis
config:
  request:
    api: /api/login
dataset:
  vars:
    user: migoo
cases:
  - title: 测试使用mybatis
    body:
      userName: '__MybatisFunc()'
      password: ${password}
      sign: ${sign}
    validators:
      - { assertion: response, field: status, expected: 200, rule: == }
